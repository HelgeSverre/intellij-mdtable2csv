# UI strings for the Markdown Table to CSV plugin.

# Settings (Settings/Preferences -> Tools -> Markdown Table to CSV)
settings.displayName=Markdown Table to CSV
settings.delimiter.label=CSV delimiter
